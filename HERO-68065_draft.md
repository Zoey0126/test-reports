# [Tech] TMS2.0 - TMS - Fix error.log with Trying to access array offset on null for TMS Module - V113 Test Report

## Functional Testing

### 1.TMS API Table Component Error Handling

#### 1.1.TMS API request path no longer writes array offset warning to error.log

**Prerequisite(s):**
1. The build with the fix for TmsApiTableComponent.php is deployed to the TMS 2.0 test environment.
2. Access to error.log on the server is available.
3. The TMS module and its API entry points are reachable.

**Step(s):**
1. Exercise the TMS API request path that goes through Plugin/Tms/Controller/Component/TmsApiTableComponent.php, using the same request payload state that previously triggered the warning.
2. Check error.log for entries produced during the request.

**Reproduction Result(s):**
1. error.log contains the warning "Trying to access array offset on null" with the location ../Plugin/Tms/Controller/Component/TmsApiTableComponent.php, line 2147.

**Fix Result(s):**
1. No "Trying to access array offset on null" warning is written to error.log.
2. The TMS API request completes normally without a server error.

#### 1.2.Normal TMS API table data request returns correct data

**Prerequisite(s):**
1. The build with the fix is deployed to the TMS 2.0 test environment.
2. A valid TMS API request for table data is available.
3. Access to error.log is available.

**Step(s):**
1. Send a standard valid TMS API request for table data that flows through TmsApiTableComponent.
2. Verify the response content returned to the caller.
3. Check error.log for any entries generated by the request.

**Reproduction Result(s):**
1. The response was returned but error.log recorded the array offset warning from the component during processing.

**Fix Result(s):**
1. The response returns the correct table data as before the fix.
2. error.log remains clean with no array offset warning for the request.

#### 1.3.TMS API request with missing or null fields is handled gracefully

**Prerequisite(s):**
1. The build with the fix is deployed to the TMS 2.0 test environment.
2. The ability to send a crafted TMS API request with missing or null fields that flow into the affected code path.
3. Access to error.log is available.

**Step(s):**
1. Send a TMS API request with missing or null fields that feed the array access at the affected location in TmsApiTableComponent.
2. Observe the response returned to the caller.
3. Check error.log for entries generated by the request.

**Reproduction Result(s):**
1. The missing or null fields caused the "Trying to access array offset on null" warning to be written to error.log.

**Fix Result(s):**
1. The request is handled gracefully with a proper validation response instead of a hidden warning.
2. No array offset warning appears in error.log.

### 2.Error Log Cleanliness

#### 2.1.error.log contains no array offset warnings after a full TMS regression run

**Prerequisite(s):**
1. The build with the fix is deployed to the TMS 2.0 test environment.
2. Access to error.log is available.
3. The TMS regression flows for the reservation module are prepared.

**Step(s):**
1. Run the typical TMS flows, including reservation list retrieval, table data fetch, reservation creation, and reservation update.
2. Open error.log and search for occurrences of "Trying to access array offset on null" produced during the regression run.

**Reproduction Result(s):**
1. Warning entries pointing to TmsApiTableComponent.php line 2147 appeared in error.log during the regression run.

**Fix Result(s):**
1. No new "Trying to access array offset on null" occurrences appear in error.log after the regression run.

### 3.Regression Scope

The following related modules and scenarios should be retested after the fix:

1. All TMS API endpoints that read table data through Plugin/Tms/Controller/Component/TmsApiTableComponent.php.
2. Reservation-related TMS flows that consume the component output, including reservation list retrieval and table data fetch.
3. Reservation creation and update flows in TMS 2.0 to confirm no functional regression from the null-check change.
4. error.log monitoring during the full TMS regression to ensure no new warnings or notices are introduced.
